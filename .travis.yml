language: python
python:
    - "2.7"
install:
    - python arpeggio/setup.py install
script:
    - mkdir bin
    - cp -r domm_peg/*.py bin
    - cp -r domm_peg/*.domm bin
    - ls
    - python bin/domm.py bin/basic.domm

